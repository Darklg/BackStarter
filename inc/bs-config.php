<?php

/* ----------------------------------------------------------
  Default lang
---------------------------------------------------------- */

if ( !defined( 'BS_LANG' ) ) {
    define( 'BS_LANG', 'en_EN' );
}

/* ----------------------------------------------------------
  Default Site Name
---------------------------------------------------------- */

if ( !defined( 'BS_NAME' ) ) {
    define( 'BS_NAME', 'BackStarter' );
}
